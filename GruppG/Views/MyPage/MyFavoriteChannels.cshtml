@model GruppG.Models.ViewModels.FavoritChannelVM

@{
    ViewBag.Title = "Mina favoritkanaler";
}

<h4>
    Dina favoritkanaler
    @Html.DisplayFor(model => model.PersonP.FirstName)
    @Html.DisplayFor(model => model.PersonP.LastName) :
</h4>

@using (Html.BeginForm())
{
    
     @Html.AntiForgeryToken()
    <table class="table">
        @foreach (var item in Model.FavoriteChannelsVM)
        {
            <tr>
                <td>
                    <h2> @item.Chanel1.Name</h2>
                </td>
                <td>
                    @Html.ActionLink("Ta bort", "MyFavoriteChannels", new { @id = item.Id })
                    <div class="form-group">
                        <div class="col-md-offset-2 col-md-10">
                            <input type="submit" value="Ta bort som favoritkanal" name="action" class="btn btn-default" />
                        </div>
                    </div>
                </td>
            </tr>
        }
    </table>




<h2>Alla kanaler</h2>
    
        <table class="table">


            @foreach (var item in Model.ChannelListVM)
            {
                

                <tr>
                    <td>
                        <h2> @item.Name </h2>
                    </td>

                    <td>
                        @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                        @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                        @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
                        @Html.ActionLink("Ta bort", "MyFavoriteChannels", new { @chan = item.Id })
                        <input type="submit" value="Lägg till" @*name="action"*@ class="btn btn-default" />
                    </td>
                </tr>
                    
                }
        </table>
       }

        @Html.ActionLink("Tillbaka", "Index", "MyPage", new { @id = Model.PersonP.Id }, null)
        @*@Html.ActionLink("Lägg till favoritkanaler", "MyFavoriteChannels", "MyPage", new { @id = Model.Id }, null)*@


    

