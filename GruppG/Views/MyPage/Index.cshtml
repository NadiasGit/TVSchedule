@model GruppG.Models.db.Person
    @{
        ViewBag.Title = "Index";
    }

    <h2>Index</h2>

<h2>Välkommen till mina sidor  @Model.FirstName @Model.LastName</h2>

<h4></h4>

<div>
    <hr />
    <dl class="dl-horizontal">

        <dt>
            <strong>Förnamn:</strong>
        </dt>

        <dd>
            @Html.DisplayFor(model => model.FirstName)
        </dd>

        <dt>
            <strong>Efternamn</strong>
        </dt>

        <dd>
            @Html.DisplayFor(model => model.LastName)
        </dd>

        <dt>
            <strong>Användarnamn</strong>
        </dt>

        <dd>

            @Html.DisplayFor(model => model.UserName)
        </dd>

    </dl>
    @Html.ActionLink("Lägg till favoritkanaler", "MyFavoriteChannels", "MyPage", new { @id = Model.Id }, null)

</div>

    @*<table class="table">
        <tr>
            <th>
                <p>Person</p>
            </th>

            @*<th>
                    <p>Kanal</p>
                </th>*@

            @*<th>
                <p>Datum</p>
            </th>

        </tr>*@

        @*@foreach (var item in Model)
        {
            <tr>

                <td> @item.FirstName</td>

                <td> @item.LastName</td>

                <td> @Html.ActionLink("Mer information", "ProgramDetails", "MyPage", new { @id = item.Id }, null)</td>

                <td> @Html.ActionLink("Lägg till favoritkanaler", "MyFavoriteChannels", "MyPage", new { @id = item.Id }, null)</td>



            </tr>

        }

    </table>*@

